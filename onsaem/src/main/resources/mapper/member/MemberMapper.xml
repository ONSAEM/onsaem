<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.onsaem.web.member.mapper.MemberMapper">

	<!-- 로그인 아이디, 비밀번호 찾기 -->
	<select id="getMember" resultType="MemberVO" parameterType="String">
		SELECT * FROM MEMBERS
		WHERE MEMBER_ID = #{memberId}
	</select>
	
	<!-- 아이디체크 -->
	<select id="checkId" resultType="MemberVO"
		parameterType="String">
		SELECT MEMBER_ID
		FROM MEMBERS
		WHERE MEMBER_ID = #{memberId}

		UNION ALL

		SELECT MEMBER_ID
		FROM APPLY_MEMBERS
		WHERE MEMBER_ID = #{memberId}
	</select>
	
	<insert id="insertMember" parameterType="MemberVO">
	INSERT INTO MEMBERS (MEMBER_ID, PASSWORD, NAME, NICKNAME, EMAIL, PHONE, POSTAL_CODE, ADDR, DETAIL_ADDR, BANK, BANK_ACCOUNT, ROLE)
	VALUES(#{memberId}, #{password}, #{name}, #{nickname}, #{email}, #{phone}, #{postalCode}, #{addr}, #{detailAddr}, #{bank}, #{bankAccount}, 'ROLE_USER')
	</insert>
</mapper>